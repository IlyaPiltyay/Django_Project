[project]
name = "django-project"
version = "0.1.0"
description = ""
authors = [
    {name = "Ilya",email = "ilya.piltyay@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django (>=5.2.2,<6.0.0)",
    "dotenv (>=0.9.9,<0.10.0)",
    "psycopg2-binary (>=2.9.10,<3.0.0)",
    "pillow (>=11.2.1,<12.0.0)",
    "ipython (>=9.3.0,<10.0.0)",
    "python-dotenv (>=1.1.1,<2.0.0)",
    "redis (>=6.2.0,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "django-project"  # Убедитесь, что это имя соответствует вашей папке
version = "0.1.0"
description = ""
authors = ["Ваше Имя <ваш.email@example.com>"]
packages = [{ include = "ilya.piltyay@gmail.com" }]